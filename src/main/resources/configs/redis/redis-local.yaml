spring:
  data:
    redis:
      host: redis
      port: 6379
      timeout: 3s

      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: 1s

  redis:
    repositories:
      enabled: false